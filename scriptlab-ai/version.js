/* version.js — Single source of truth for cache versioning.
   Update this number when you change any cached file.
   Then update: sw.js CACHE name, and all ?v= query strings in index.html. */
var VERSION = 18;